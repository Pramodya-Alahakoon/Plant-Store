<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Leafy" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Leafy" parent="Base.Theme.Leafy" />

    <style name="Theme.YourAppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Add these items -->
        <item name="textInputStyle">@style/CustomTextInputLayout</item>
        <item name="editTextStyle">@style/CustomTextInputEditText</item>
    </style>
</resources>